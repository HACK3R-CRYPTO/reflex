import { getAddress, toBytes, createPublicClient, webSocket, toFunctionSelector, zeroAddress, toEventSelector, numberToHex, isAddress, isAddressEqual } from 'viem';

// src/services/viem/index.ts
var y = class {
  constructor(e) {
    this.chainId = 0;
    this.client = e;
  }
  async getChainId() {
    return this.chainId === 0 && (this.chainId = await this.client.public.getChainId()), this.chainId;
  }
  async readContract(e, t, i, r = []) {
    return this.client.public.readContract({
      address: e,
      abi: t,
      functionName: i,
      args: r
    });
  }
  async writeContract(e, t, i, r = [], s = BigInt(0)) {
    if (!this.client.wallet)
      return null;
    let n = this.client.wallet.account ?? null;
    return this.client.wallet.writeContract({
      address: e,
      abi: t,
      functionName: i,
      args: r,
      value: s,
      account: n,
      chain: this.client.wallet.chain
    });
  }
  async waitForTransaction(e) {
    return this.client.public.waitForTransactionReceipt({
      hash: e
    });
  }
  async getCurrentAccounts() {
    if (!this.client.wallet)
      throw new Error("No wallet client");
    let e = [];
    if (this.client.wallet.account)
      e.push(this.client.wallet.account.address);
    else
      try {
        e = await this.client.wallet.getAddresses();
      } catch {
      }
    if (e.length === 0)
      throw new Error("No wallets detected");
    return e;
  }
};

// src/abi/SomniaReactivityPrecompile.ts
var p = [{ type: "function", name: "getSubscriptionInfo", inputs: [{ name: "subscriptionId", type: "uint256", internalType: "uint256" }], outputs: [{ name: "subscriptionData", type: "tuple", internalType: "struct ISomniaReactivityPrecompile.SubscriptionData", components: [{ name: "eventTopics", type: "bytes32[4]", internalType: "bytes32[4]" }, { name: "origin", type: "address", internalType: "address" }, { name: "caller", type: "address", internalType: "address" }, { name: "emitter", type: "address", internalType: "address" }, { name: "handlerContractAddress", type: "address", internalType: "address" }, { name: "handlerFunctionSelector", type: "bytes4", internalType: "bytes4" }, { name: "priorityFeePerGas", type: "uint64", internalType: "uint64" }, { name: "maxFeePerGas", type: "uint64", internalType: "uint64" }, { name: "gasLimit", type: "uint64", internalType: "uint64" }, { name: "isGuaranteed", type: "bool", internalType: "bool" }, { name: "isCoalesced", type: "bool", internalType: "bool" }] }, { name: "owner", type: "address", internalType: "address" }], stateMutability: "view" }, { type: "function", name: "subscribe", inputs: [{ name: "subscriptionData", type: "tuple", internalType: "struct ISomniaReactivityPrecompile.SubscriptionData", components: [{ name: "eventTopics", type: "bytes32[4]", internalType: "bytes32[4]" }, { name: "origin", type: "address", internalType: "address" }, { name: "caller", type: "address", internalType: "address" }, { name: "emitter", type: "address", internalType: "address" }, { name: "handlerContractAddress", type: "address", internalType: "address" }, { name: "handlerFunctionSelector", type: "bytes4", internalType: "bytes4" }, { name: "priorityFeePerGas", type: "uint64", internalType: "uint64" }, { name: "maxFeePerGas", type: "uint64", internalType: "uint64" }, { name: "gasLimit", type: "uint64", internalType: "uint64" }, { name: "isGuaranteed", type: "bool", internalType: "bool" }, { name: "isCoalesced", type: "bool", internalType: "bool" }] }], outputs: [{ name: "subscriptionId", type: "uint256", internalType: "uint256" }], stateMutability: "nonpayable" }, { type: "function", name: "unsubscribe", inputs: [{ name: "subscriptionId", type: "uint256", internalType: "uint256" }], outputs: [], stateMutability: "nonpayable" }, { type: "event", name: "BlockTick", inputs: [{ name: "blockNumber", type: "uint64", indexed: true, internalType: "uint64" }], anonymous: false }, { type: "event", name: "Schedule", inputs: [{ name: "timestampMillis", type: "uint256", indexed: true, internalType: "uint256" }], anonymous: false }, { type: "event", name: "SubscriptionCreated", inputs: [{ name: "subscriptionId", type: "uint256", indexed: true, internalType: "uint256" }, { name: "owner", type: "address", indexed: true, internalType: "address" }, { name: "subscriptionData", type: "tuple", indexed: false, internalType: "struct ISomniaReactivityPrecompile.SubscriptionData", components: [{ name: "eventTopics", type: "bytes32[4]", internalType: "bytes32[4]" }, { name: "origin", type: "address", internalType: "address" }, { name: "caller", type: "address", internalType: "address" }, { name: "emitter", type: "address", internalType: "address" }, { name: "handlerContractAddress", type: "address", internalType: "address" }, { name: "handlerFunctionSelector", type: "bytes4", internalType: "bytes4" }, { name: "priorityFeePerGas", type: "uint64", internalType: "uint64" }, { name: "maxFeePerGas", type: "uint64", internalType: "uint64" }, { name: "gasLimit", type: "uint64", internalType: "uint64" }, { name: "isGuaranteed", type: "bool", internalType: "bool" }, { name: "isCoalesced", type: "bool", internalType: "bool" }] }], anonymous: false }, { type: "event", name: "SubscriptionRemoved", inputs: [{ name: "subscriptionId", type: "uint256", indexed: true, internalType: "uint256" }, { name: "owner", type: "address", indexed: true, internalType: "address" }], anonymous: false }];

// src/abi/SomniaEventHandler.ts
var T = [{ type: "function", name: "onEvent", inputs: [{ name: "emitter", type: "address", internalType: "address" }, { name: "eventTopics", type: "bytes32[]", internalType: "bytes32[]" }, { name: "data", type: "bytes", internalType: "bytes" }], outputs: [], stateMutability: "nonpayable" }];
var c = getAddress(
  "0x0000000000000000000000000000000000000100"
), h = `0x${Buffer.from(toBytes(0, { size: 32 })).toString("hex")}`;
function w(a) {
  return /^0x[0-9a-fA-F]{64}$/.test(a);
}
function x(a) {
  return !(!isAddress(a, { strict: false }) || isAddressEqual(a, zeroAddress));
}
function u(a) {
  return isAddressEqual(a, c);
}

// src/modules/Reactivity.ts
var d = class {
  constructor(e) {
    this.viem = new y(e);
  }
  /**
   * Typescript event subscriptions to Solidity events which bundle any ETH call result along with event log data
   * @param param0 See SubscriptionInitParams type which defines the events being observed, the eth calls executed and callback fn to call
   * @returns The node subscription identifier and an unsubscribe callback or an Error object if the subscription fails to start
   */
  async subscribe({
    ethCalls: e,
    context: t,
    onData: i,
    onError: r,
    eventContractSources: s,
    topicOverrides: n,
    onlyPushChanges: l
  }) {
    try {
      if (this.viem.client.public.transport.type !== "webSocket")
        throw new Error("Invalid public client config - websocket required");
      return createPublicClient({
        chain: this.viem.client.public.chain,
        transport: webSocket()
        // Defaults to chain's WS URL
      }).transport.subscribe({
        params: [
          "somnia_watch",
          {
            address: s,
            // undefined means wildcard filter
            topics: n,
            // undefined means wildcard filter
            eth_calls: e,
            // must be defined but can be empty array    
            context: t,
            // undefined means no context used in eth_calls
            push_changes_only: l
          }
        ],
        onData: i,
        onError: r
      });
    } catch (o) {
      return o instanceof Error ? o : new Error(String(o));
    }
  }
  /**
   * Start a subscription to events emitted by smart contracts with the callback being handled by a Solidity smart contract
   * @param subscription Defines the parameters for the subscription, mapping to wild card defaults where optional arguments are not provided
   * @returns A transaction hash if successful or a JS Error object containing failure reason
   */
  async createSoliditySubscription(e) {
    if (!x(e.handlerContractAddress))
      return new Error("Handler contract address is invalid");
    if (u(e.handlerContractAddress))
      return new Error("Handler cannot be set to the precompile");
    if (e.maxFeePerGas === BigInt(0))
      return new Error("Max fee per gas cannot be zero");
    if (e.origin && u(e.origin))
      return new Error("Origin cannot be set to the precompile");
    if (e.caller && u(e.caller))
      return new Error("Caller cannot be set to the precompile");
    if (e.emitter && u(e.emitter))
      return new Error("Emitter cannot be set to the precompile");
    let t = e.eventTopics || [];
    if (!e.emitter && t.length === 0)
      return new Error("Emitter or event topics must be supplied at a minimum");
    if (t.length > 0 && !t.every(w))
      return new Error("All eventTopics must be valid bytes32 hex strings");
    let r = t.slice(0, 4).concat(new Array(Math.max(4 - t.length, 0)).fill(h)), s = {
      ...e,
      eventTopics: r,
      origin: e.origin ?? zeroAddress,
      caller: e.caller ?? zeroAddress,
      emitter: e.emitter ?? zeroAddress,
      handlerFunctionSelector: e.handlerFunctionSelector ?? toFunctionSelector(
        T.filter((n) => n.name === "onEvent")[0]
      )
    };
    try {
      let n = await this.viem.writeContract(
        c,
        p,
        "subscribe",
        [s]
      );
      if (!n)
        throw new Error("Failed to broadcast TX to create on-chain subscription");
      return n;
    } catch (n) {
      return n instanceof Error ? n : new Error(String(n));
    }
  }
  /**
   * Cancels an active contract event subscription being handled by a solidity smart contract
   * @param subscriptionId The unique identifier for the subscription issued on subscription creation
   * @returns A transaction hash if successful or a JS Error object containing failure reason
   */
  async cancelSoliditySubscription(e) {
    try {
      let t = await this.viem.writeContract(
        c,
        p,
        "unsubscribe",
        [e]
      );
      if (!t)
        throw new Error("Failed to broadcast TX to cancel on-chain subscription");
      return t;
    } catch (t) {
      return t instanceof Error ? t : new Error(String(t));
    }
  }
  /**
   * Create a subscription to the head of the chain reaching a specific (or any) block number
   * @param param0 The block subscription definition optionally including the block number
   * @returns Transaction hash if subscription was created or Error if a subscription was not created
   */
  async createOnchainBlockTickSubscription({
    blockNumber: e,
    handlerContractAddress: t,
    handlerFunctionSelector: i,
    priorityFeePerGas: r,
    maxFeePerGas: s,
    gasLimit: n,
    isGuaranteed: l,
    isCoalesced: o
  }) {
    let m = toEventSelector({
      name: "BlockTick",
      type: "event",
      inputs: [
        { type: "uint64", indexed: true, name: "blockNumber" }
      ]
    }), b = e ? numberToHex(e, { size: 32 }) : h, A = {
      eventTopics: [
        m,
        b
      ],
      emitter: c,
      handlerContractAddress: t,
      handlerFunctionSelector: i,
      priorityFeePerGas: r,
      maxFeePerGas: s,
      gasLimit: n,
      isGuaranteed: l,
      isCoalesced: o
    };
    return this.createSoliditySubscription(A);
  }
  /**
   * Schedule a smart contract callback in the future
   * @param param0 Subscription definition including the timestamp in ms
   * @returns Transaction hash if the cron was successfully scheduled or Error if a subscription was not created
   */
  async scheduleOnchainCronJob({
    timestampMs: e,
    handlerContractAddress: t,
    handlerFunctionSelector: i,
    priorityFeePerGas: r,
    maxFeePerGas: s,
    gasLimit: n,
    isGuaranteed: l,
    isCoalesced: o
  }) {
    let m = toEventSelector({
      name: "Schedule",
      type: "event",
      inputs: [
        { type: "uint256", indexed: true, name: "timestampMilis" }
      ]
    });
    if (e < (/* @__PURE__ */ new Date()).getTime() + 12 * 1e3)
      return new Error("Invalid timestamp - not in the future");
    let b = {
      eventTopics: [
        m,
        numberToHex(e, { size: 32 })
      ],
      emitter: c,
      handlerContractAddress: t,
      handlerFunctionSelector: i,
      priorityFeePerGas: r,
      maxFeePerGas: s,
      gasLimit: n,
      isGuaranteed: l,
      isCoalesced: o
    };
    return this.createSoliditySubscription(b);
  }
  /**
   * Gets the detailed parameters of a Solidity subscription given a unique subscription identifer
   * @param subscriptionId Subscription identifier
   * @returns The populated subscription or JS Error if the query failed
   */
  async getSubscriptionInfo(e) {
    try {
      return this.viem.readContract(
        c,
        p,
        "getSubscriptionInfo",
        [e]
      );
    } catch (t) {
      return t instanceof Error ? t : new Error(String(t));
    }
  }
};

// src/index.ts
var g = class extends d {
  /**
   * Create a new SDK instance
   * @param client Viem wrapper object for consuming the public client and optionally the wallet client for transactions
   */
  constructor(e) {
    super(e);
  }
};

export { d as Reactivity, g as SDK };
//# sourceMappingURL=index.js.map
//# sourceMappingURL=index.js.map